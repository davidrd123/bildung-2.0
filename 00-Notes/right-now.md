# Right Now

Last updated: 2026-03-31

## Proven

- The repo now has a usable layer split: `patterns/charter.md` states purpose, `00-Notes/process.md` states workflow, and `patterns/handle-schema.md` states reference grammar.
- The first handle pilot is real in `texts/zeitmauer/source/handles.yaml` for translated `§§1-16`.
- Evidence chains are real, not just planned. `term:schicksalszeit`, `term:hinzutretende`, and `term:da-sein-so-sein` already have machine-readable grounding.
- The first cross-project thread pilot is real: `thread:time-crisis` has a workable traversal from Jünger passages into journal commentary and synthesis.
- Symbiotic-Vault interoperability is now concrete at the handle layer through loose-coupled `vault:*` and `b2:*` references.

## Current

- The active architectural hardening is the honesty layer, not a bigger ontology.
- `texts/zeitmauer/` is the most structurally advanced subproject: translated `§§1-16`, glossary accreting, handle pilot live, thread extraction beginning.
- `thread:time-crisis` is the main current test of whether handles, evidence chains, notes, and synthesis can form a traversable object rather than a prose-only theme.

## Next

- Extend the handle and evidence pilot through more `zeitmauer` material before widening the object model.
- Keep crystallizing only what the pilot forces: likely relation vocabulary next, then broader object guidance only if `handle-schema.md` becomes overloaded.
- Seed additional threads or frames only after the current pilot proves reusable across rereading.

## Live Question

- How much honesty and verification structure is enough before the project genuinely needs more ontology?
- More concretely: is the current `handle -> evidence -> thread` pilot enough to support rereading and synthesis, or does it already force a fuller object layer?

## Do Not Do Yet

- Do not build a dashboard.
- Do not merge ontologies with Symbiotic-Vault.
- Do not build the dynamic view layer before handle practice stabilizes.
- Do not promote anything to `stable` on agent fluency alone.
