# Packet 03: `thread:time-crisis` x `thread:orthogonal-time` — temporal occlusion?

Purpose: preserve the actual packet used for the first bounded collision between Jünger's `thread:time-crisis` and Dick's `thread:orthogonal-time`, so it can be rerun or handed off without reconstructing it from the result note.

Status: as-run packet record. This is a packet note, not a result note.

## Task

Using the thread dossiers, local passage surfaces, and one prison/occlusion support thread below, answer this question:

What happens when `thread:time-crisis` meets `thread:orthogonal-time` under replay discipline?

More narrowly:

- do both dossiers track a shared structure better described as **temporal occlusion**
- if they do, is the shared structure at the level of relation between false time and real time, or only at the level of loose anti-modern atmosphere
- what direct pairing fails and should be refused

## Constraints

- Do not write a Dick/Jünger synthesis.
- Do not collapse `Schicksalszeit` into orthogonal time.
- Keep the comparison at the level of temporal relation, not whole cosmology.
- Require one bridge sentence and one useful negative.
- Treat `temporal occlusion` as a hypothesis under test, not as a handle proposal.
- Do not import modern vocabulary (`sheaf`, `topos`, Peirce, Hoffmeyer) into the result.

## Packet

### Jünger side

- `texts/zeitmauer/threads/time-crisis.md`
- `texts/zeitmauer/parts/02-messbare-und-schicksalszeit.md`
- `texts/zeitmauer/second-pass-messbare-und-schicksalszeit.md`

Load-bearing local pressure:

- `§4`: man values `Da-Sein` more than `So-Sein`
- the `Meßbare und Schicksalszeit` arc: measurable time widens from astrology into planning, accident, periodicity, and mass uniformization

### Dick side

- `texts/exegesis/threads/orthogonal-time.md`
- `texts/exegesis/threads/black-iron-prison.md`
- `texts/exegesis/passes/19-folder-21.md`
- `texts/exegesis/passes/45-folder-90.md`

Load-bearing local pressure:

- fake time / counterfeit duration / spurious interpolation
- prison as occlusion, not only domination
- late correction: the occlusion is lifted from us, not from the world

## Output Contract

Return:

1. a short result statement
2. one constrained bridge sentence
3. what on the Jünger side carries the bridge
4. what on the Dick side carries the bridge
5. one useful negative where direct pairing fails
6. a provisional judgment on whether the packet worked
7. what the result does not license

## Non-Goals

- Do not create a new cross-project handle.
- Do not treat `outside-in / inside-out` as the main result unless the packet forces it.
- Do not decide whether Dick's ontological claim is finally true.
- Do not widen from these packet surfaces to a full theology of history.
