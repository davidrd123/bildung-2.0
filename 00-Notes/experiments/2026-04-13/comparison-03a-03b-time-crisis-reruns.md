# Comparison: `03A` vs `03B` on `thread:time-crisis` x `orthogonal-time.md`

Purpose: preserve the minimal comparison between the two passage-scale reruns so the convergence and the shift in contact-level do not have to be reconstructed later from the separate experiment notes.

Status: working-layer comparison. Not a new collision format. Not a cross-project note. Not authority for a handle or frame.

## Inputs

- `experiment-03a-time-crisis-passage-scale-rerun.md`
- `experiment-03b-time-crisis-historical-frame-rerun.md`

## What converged

Both reruns say the contact is real, but only at the level of **relation**, not at the level of shared term or shared ontology.

Both preserve the same refusals:

- `Schicksalszeit` is not `orthogonal time`
- `Zeitmauer` is not Dick's orthogonal axis
- no shared cosmology is yet earned
- no new cross-project handle is justified

Both also converge on the Dick-side pressure:

- counterfeit elapsed time
- a more-real order hidden by substitute duration
- the late correction that the blockage is lifted from us rather than from the world

## What changed

`03A` used `§4` and `§102`. Its strongest result was:

- the contact survives as a relation between **substitute time** and a **more-real order**

`03B` used `§47-48`. Its strongest result was:

- the contact survives as a relation of **frame-failure**, where inherited temporal language no longer fits while a more-real order presses through it

So the second rerun did not overturn the first. It sharpened it.

The current best read is:

- `03A` catches the temporal relation in its stronger contrast form
- `03B` catches what happens to reading and naming once that relation begins to pressure the inherited frame

## Provisional takeaway

The contact looks more than atmospheric because it survives across two different Jünger pressure clusters.

But what survives is still modest:

- not term-equivalence
- not metaphysical identity
- not a stable cross-project vocabulary

What survives is:

- a recurring relation in which operative or inherited time covers, misreads, or fails before a more-real order

## What this does not license

- not promotion of `temporal occlusion` as the settled phrase
- not promotion of `frame-failure` as the settled phrase
- not a cross-project thread handle
- not use of these two reruns alone as authority for wider Dick/Jünger synthesis

## Next Honest Move

If there is a next move, it should be one smaller rerun that tests the methodological adjacency now visible in the looser collision note:

- Jünger's typological caution under failing historical language
- Dick's receptive pattern against paranoid projection

If that smaller rerun fails, `03A` and `03B` still stand as useful bounded contacts and nothing more.
