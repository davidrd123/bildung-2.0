# Packet 03: Replay on `Umwelt` — `Merkzeichen -> Merkmal` family pressure

Purpose: preserve the third `Umwelt` replay packet as a clean handoff spec. This packet tests whether `Umwelt` still holds as a usable isolated pressure-node once Encounter 03's explicit `Merkzeichen -> Merkmal` transfer is brought into view, or whether the term can now only be understood inside a larger `Merk* / Funktionskreis / Planmäßigkeit` family.

Status: successor packet only. This is a packet note, not a result note.

## Task

Using the term state, prior `Umwelt` replay findings, and the bounded Encounter 03 surfaces below, answer this question:

What does the explicit `Merkzeichen -> Merkmal` sequence do to the current pressure on `Umwelt`?

More narrowly:

- does it deepen one of the already-visible `Umwelt` pressures
- does it introduce a genuinely different kind of pressure
- does it show that `Umwelt` is only stable as a node inside a larger `Merk*` family

## Constraints

- Do not summarize Encounter 03 as a whole.
- Do not turn the result into a general statement about Uexküll's biology.
- Keep Experiments 01 and 02 in view, but do not force their language if the new passage shifts the center of pressure.
- Require one explicit statement of what survives unchanged and one explicit statement of what shifts.
- Say directly whether `Umwelt` still looks like the right isolated pressure target, or whether the family has become the real unit.
- Do not promote any English rendering.
- Do not treat `Merkzeichen`, `Merkmal`, `Merkwelt`, and `Umwelt` as separable if the passage itself refuses that separation.

## Term Under Pressure

- `term:umwelt`
- source: `sources/german/uexkuell-theoretische-biologie/glossary.yaml`

Current state before replay:

- `preferred: null`
- alternatives:
  - `environment`
  - `surrounding world`
  - `own-world`
- strongest current note:
  - not a neutral external container
  - should not harden into a merely private bubble
  - the world-side is planfully answering rather than merely surrounding
  - for us it is available only as a constrained reconstruction, not as an adoptable inner viewpoint

## Older Findings In Play

From `sources/german/uexkuell-theoretische-biologie/encounters/05-welt-und-umwelt.md`:

- `Umwelt` culminates not in isolation but in a dense weave of overlapping and partly contradictory worlds
- the strongest check against privacy is that a body belongs to many worlds under different functional cuts

From `experiment-01-umwelt-planfulness-outside-the-body.md`:

- `Umwelt` is not just centered world but the planfully answering outer segment of the functional circle

From `experiment-02-umwelt-anti-psychologism-and-reconstruction.md`:

- `Umwelt` is available to us only as a constrained reconstruction within our own apperceptive unity
- the term currently looks like a junction carrying at least two live pressures:
  - answering structure
  - reconstruction constraint

## New Passage For Replay

From `sources/german/uexkuell-theoretische-biologie/encounters/03-merkzeichen-und-umwelten.md`:

Primary load-bearing lines:

> `So gefaßt, bedeutet das Merkzeichen die eben merkliche Inhaltsänderung der Aufmerksamkeit.`

> `Setzt man an Stelle von Empfindung oder subjektiver Qualität das Merkzeichen, so kann man sagen, die Merkzeichen unserer Aufmerksamkeit werden zu Merkmalen der Welt.`

> `Daher müssen die Gesetze, die für die inneren Merkzeichen bindend sind, auch für die äußeren Merkmale gelten.`

Support pressure from the same encounter:

> `Die Gesetzmäßigkeit, die ein jedes Subjekt mit seiner Umwelt verbindet, kann nicht durch bloße Kausalität erfaßt, sondern nur als Planmäßigkeit gedeutet werden.`

## Output Contract

Return:

1. a short result statement
2. what survived unchanged
3. what shifted
4. whether the packet makes `Umwelt` look more isolable or less isolable
5. the new residue made visible in the English alternatives
6. a provisional judgment on whether the replay packet worked
7. what the result does not license

## Baseline To Beat

The cheap result would be:

> Encounter 03 just confirms that `Umwelt` is part of a larger chain and therefore remains difficult.

This run only counts if it produces:

- a sharper account of what the `Merk*` transfer changes in the pressure on `Umwelt`
- a real judgment about whether the family has become the operative unit
- one more precise diagnosis of what the current English candidates fail to carry under this family pressure

## Non-Goals

- Do not settle `Umwelt`.
- Do not settle `Merkzeichen` or `Merkmal`.
- Do not widen into a full theory of supra-subjective lawfulness.
- Do not infer a standing method from three packets alone.
- Do not create a new glossary architecture unless the result truly forces it.
