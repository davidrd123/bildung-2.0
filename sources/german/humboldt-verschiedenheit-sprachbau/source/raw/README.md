# Raw Extracts

This directory is for uncorrected page or section extracts after witness
alignment has been checked.

Use raw files to preserve the shape of the OCR or PDF extraction that produced
them. Do not silently correct spelling, hyphenation, or OCR errors here. Put
checked working text in `../normalized/` only when a bounded source task needs
it.
