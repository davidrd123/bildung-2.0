# Raw Extracts

Raw OCR and PDF text extracts live here.

Use these as witnesses, not as corrected source text. Preferred scan extracts should normally come from the version2 JP2 files with `scripts/ocr_jp2_page.py`; the Google / IA PDF can be extracted with `scripts/extract_pdf_page_range.py` when quick searchability matters.

