# Image-Grounded Cleaned Sections

This directory contains cleaned section files assembled from direct page-image
transcripts in `source/page-transcripts/`.

These files are distinct from `source/cleaned/`, which was generated from the
PDF text layer. For image-grounded sections, the rendered page image is the
authority and OCR/text-layer output is only a draft witness.
